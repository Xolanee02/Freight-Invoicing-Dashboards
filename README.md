# Freight Invoicing Dashboards

Simple static web app with a freight invoice factoring intake form and local admin dashboard.

## Run locally

```bash
python3 -m http.server 4173
```

Then open `http://localhost:4173`.
